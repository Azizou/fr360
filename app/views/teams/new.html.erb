<% @page_title='New team' %>
<div class="container">
	<h2>Create a new team</h2>
	<%= form_for @team, url: teams_path do |f| %>
		<%= render partial: 'form', locals:{ f: f} %>
		<%= f.submit('Save new team', class: 'btn btn-info') %>
	<% end %>
</div>
